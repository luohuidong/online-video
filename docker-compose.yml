services:
  server:
    build:
      context: .
      target: server
    ports:
      - '3000:3000'
    volumes:
      # SQLite data persistence
      - online-video-data:/app/.data
      # Mount config file (edit config.yml to configure sources)
      - ./config.yml:/app/config.yml:ro
    restart: unless-stopped

  web:
    build:
      context: .
      target: web
    ports:
      - '80:80'
    depends_on:
      - server
    restart: unless-stopped

volumes:
  online-video-data:
